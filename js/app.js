// Récupérer id de l'article (mettre onclick dans le DOM)
// function setData(idArticle) {
//     localStorage.setItem('articleSolo', idArticle);
// };



// function convertPrice(price) {

//     const priceEuro = (price / 100).toFixed(2);
//     const priceToStr = priceEuro.toString();
//     let finalPrice = 0

//     let lastChar = priceToStr.charAt(priceToStr.length-1);
    
//     if (lastChar !== 0 || lastChar !== "0") {
//         lastChar = lastChar + "0";
//     } else {
//         lastChar = lastChar;
//     }

//     console.log(lastChar);

// };

// convertPrice(532);